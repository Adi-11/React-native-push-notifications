// @flow
import firebase from 'react-native-firebase';
// Optional flow type
import RemoteMessage from 'react-native-firebase';

export default async (message: typeof RemoteMessage) => {
  // handle your message

  return Promise.resolve();
};
